# rubevy documents

The layout every repository of the organization uses is described in
[sabiruby/.github/CONTRIBUTING.md](https://github.com/sabiruby/.github/blob/main/CONTRIBUTING.md).
This repository is small, so its documents sit flat here; the worklog has its own directory.

What changed in each release, with the commit behind every claim, is
[`../CHANGELOG.md`](../CHANGELOG.md) at the root of the repository.

| file | language | what it is |
|---|---|---|
| [host-api.md](host-api.md) | English | what a script can say to the game and the game to a script: `Rubevy.ask`, where the game's systems go in the frame (`RubevySet`), what a question may carry (`Arg`, including a Hash or an Array), answers (including an object of the game's own, one made by a future, and one given by a closure inside the tick — `answer_in_tick`, no frame at all), entities, components by name (a read is answered inside the tick that asked it; a write still lands at the end of the frame), events, `Rubevy::Proxy`, adding to the VM at `Startup`, time limits and pausing, running a second VM for mods under a name tag, replacing a script |
| [rust-bridge.ja.md](rust-bridge.ja.md) | Japanese | how a robot's question travels through rubevy and the game and back, point by point against embedding the C mruby |
| [outlook.md](outlook.md) | English | what rubevy builds on SabiRuby, in order, with the status of each item; the honest comparison with Lua; the possibilities |
| [outlook.ja.md](outlook.ja.md) | Japanese | the possibilities and the status, in plain Japanese |
| [plans/ecs-bridge-plan.md](plans/ecs-bridge-plan.md) | Japanese | the instructions for the ECS bridge (components by name through reflection) and the event queue; status inside |
| [plans/sync-access-plan.md](plans/sync-access-plan.md) | (Japanese) component reads that return in the same tick, with no unsafe and no change to sabiruby: an exclusive tick that runs the VM, answers the reads it parked with `&World`, and runs it again; writes left as they are; stages S1–S5, status inside |
| [plans/multi-vm-plan.md](plans/multi-vm-plan.md) | Japanese | the instructions for more than one VM in one app: what was already per-VM, the six places that assumed one, three designs (the type marker chosen), the stages; the result is in `host-api.md` and `examples/two_vms.rs` |
| [plans/publish-plan.md](plans/publish-plan.md) | Japanese | the instructions for the first release on crates.io: the version floors for the SabiRuby crates and where each number comes from, why rubevy_games keeps one VM with `[patch.crates-io]` rather than following rubevy onto crates.io, the stages P1–P3; status inside |
| [plans/generalize-plan.md](plans/generalize-plan.md) | Japanese | to do: what the measurements of 2026-09-20 asked for (an index over subscriptions, one irep for one `.mrb`, a count of dropped messages and a `limit:`, `frame_time` made a real bound, `FrameStats` and two standing examples that measure how many scripts and subscribers a machine carries), the entry points both sample games wrote by hand (a program with a prelude and its line numbers, swapping a script, an embedded `Host`, embedding `.rb` at build time, collecting declarations — which goes to `sabiruby-serde`), and what a script can reach (resources by name, an optional Ruby layer starting with the camera); and every number rubevy already holds made settable with its reason (or "unknown") written down in `docs/numbers.md`; stages R0–R10, status inside |
| [worklog/](worklog/) | Japanese | dated records of work: what was read, tried, decided |

The worklog, newest last:

| file | what it records |
|---|---|
| [worklog/2026-09-15-host-state-and-data.md](worklog/2026-09-15-host-state-and-data.md) | the command queue moved into the VM's host state, and entities as Data objects |
| [worklog/2026-09-15-stage3b-no-internal-access.md](worklog/2026-09-15-stage3b-no-internal-access.md) | the four places that reached into `Vm`'s fields, moved to the VM's own entry points |
| [worklog/2026-09-15-stage6bc-futures-proxy.md](worklog/2026-09-15-stage6bc-futures-proxy.md) | `answer_with` (a request answered from a future), and why the dynamic proxy of stage 6c stopped at the VM's C-function boundary |
| [worklog/2026-09-15-stage6c-proxy.md](worklog/2026-09-15-stage6c-proxy.md) | the dynamic proxy finished once the VM stopped putting a boundary around `method_missing`: what `proxy.rb` is, and what it does not say about itself |
| [worklog/2026-09-15-ecs-bridge.md](worklog/2026-09-15-ecs-bridge.md) | components by name through Bevy's reflection: what the plan assumed about `Vec3` and `TransformPlugin`, and why the read is `get` and not `[]` |
| [worklog/2026-09-15-events.md](worklog/2026-09-15-events.md) | events as queues: where a subscription is let go of, the 64-message limit, and the two tests that were racing the schedule |
| [worklog/2026-09-15-entity-index.md](worklog/2026-09-15-entity-index.md) | `e[:Transform]` once the VM stopped putting a boundary around `OP_GETIDX`: what changed in sabiruby, and why `get` stayed |
| [worklog/2026-09-16-arg-value.md](worklog/2026-09-16-arg-value.md) | a Hash or an Array as an argument of `Rubevy.ask`: why the value travels rather than a copy of it, and the two-step release that a `Drop` without a `Vm` needs |
| [worklog/2026-09-16-bridge-followups.md](worklog/2026-09-16-bridge-followups.md) | the three the bridge left behind: `funcall` replaced by the VM's own entry points, a `Task.new` task carrying the script's entity, and the closed queue that ends a waiting task |
| [worklog/2026-09-17-scheduling-and-vm-access.md](worklog/2026-09-17-scheduling-and-vm-access.md) | `RubevySet` (where a game's answering system goes, and why two frames was luck rather than a rule), a pause that does not spend a `sleep`, and the two things the next game needed that turned out to be there already |
| [worklog/2026-09-17-restart-burst.md](worklog/2026-09-17-restart-burst.md) | ten scripts replaced in one frame froze the whole VM: what rubevy's own way of closing a subscription had to do with it, the sixty lost frames that matched ten creatures' sixty reflex tasks exactly, and why the fix belonged in the VM |
| [worklog/2026-09-17-multi-vm.md](worklog/2026-09-17-multi-vm.md) | a name tag on the VM, and then a second VM behind it (the whole multi-VM plan): why a default type parameter is filled in where a type is written but not where a value is, what that cost `RubevySet` (an enum no longer), which derives really add bounds to a generic, the component hook that took `::<M>` after all, the two tests that failed for the right reasons — a script restarted by the very removal meant to end it, and a guard whose absence nothing could see until an asset was added between the two plugins — why `publish` could not have crossed VMs even if it had wanted to, and the mod in `examples/two_vms.rs` that spins three million instructions without costing the game a beat |
| [worklog/2026-09-17-sync-access-survey.md](worklog/2026-09-17-sync-access-survey.md) | the survey a plan for synchronous component access rests on (no code touched): what `tick_scripts` is and what a native is handed (`&mut Vm` and a host state that must be `Send + Sync + 'static`), the whole road a `component.get` travels and why it costs one frame, what could become synchronous and what should not (structural changes, the questions a game answers, futures), what lending `&mut World` to the tick would take in bevy 0.19 (`resource_scope` really takes the resource out, which is the safety argument) and what it would cost (an exclusive system, the first `unsafe`), where the frame goes in the reflection lookup, the places in the three sample games that are built on the one frame, and the tests and documents that would have to be rewritten |
| [worklog/2026-09-17-sync-reads.md](worklog/2026-09-17-sync-reads.md) | the reads made synchronous without an `unsafe` (S1 of the plan): the tick as an exclusive system that runs the VM, answers the reads its scripts parked on out of the `&World` it is holding and runs them again; why the loop needs no round limit of its own (the instruction budget already bounds it — 75 instructions a read); the `ReflectComponent` cache that also settled a borrow; what a read costs now (2.2 µs, and 2667 of them in one frame where there used to be one) and what a write still promises |
| [worklog/2026-09-17-in-tick-answers.md](worklog/2026-09-17-in-tick-answers.md) | the game's own answers given inside the tick (S4 of the plan): a kind of `Rubevy.ask` handed to a closure the answer loop calls between two runs of the VM, sorted out of `take_requests` the way rubevy's own four are; why the map of answerers is moved out of `ScriptWorld` for the length of the call and what was dropped instead (an `Arc`, a vector of answers); what a closure cannot do — no `Vm`, so the flat `Answer` only and an `Arg::Value` it can see but not read into; the test that a `.before(RubevySet::Tick)` system is what the closure sees, checked by moving it after; and the example where the same script waits 0 frames for the game's closure and 1 for the game's system |
| [worklog/2026-09-18-wasm-instant.md](worklog/2026-09-18-wasm-instant.md) | ブラウザで庭が真っ黒になった 1 行: S1 が答えループに入れた `std::time::Instant::now()` は `wasm32-unknown-unknown` で `time not implemented on this platform` と panic する — console の実文と、黒画面を「黒い」と言うための計測（canvas が `300×150` のまま、16000 画素すべて 0）、`git log -S` が指した 1 コミット、そして VM 自身に `task_set_clock` で渡してある時計（`clock_ns`、Bevy の `Instant`）に替えた理由（「1 行で済む `bevy::platform::time::Instant::now()`」を採らなかったのは、`RunLimits::time_ns` を測り直す側と同じ源であるべきだから）。`cargo build --target wasm32-unknown-unknown` は**バグのあるコードでも通っていた**ので、wasm ビルドを CI に足すだけでは捕まらないこと、機械が混んでいて落ちた `tests/pause.rs` が変更とは無関係だったこと、そして「ネイティブで通ってブラウザで死ぬ」をこれから止める 2 つの案（src/ の grep か、wasm で 1 フレーム回す仕掛けか）が著者判断待ちであること |
| [worklog/2026-09-18-wasm-guard.md](worklog/2026-09-18-wasm-guard.md) | 黒画面の 1 行と同じ形をソースの側で止めるテスト（`tests/no_wasm_unsupported.rs`）: ビルドが素通りする以上「書かれている字」を見るしかないこと（一時的に `std::time::Instant::now()` を戻すとテストは落ち、**同じ状態で wasm ビルドは通る**）、`bevy::platform::time::Instant` と std のそれを use 文の平坦化で見分ける理由（`use std::time::{Duration, Instant}` は生の grep に当たらない）、コメント行を飛ばす理由（昨日の修正が残した rustdoc が `std::time::Instant` を説明している）、cfg を解析せず `// wasm: <理由>` を書かせる理由、`src/` に当たった 1 行（`FileHost::read_file` の `std::fs::read` — ブラウザにファイルシステムが無いのは仕様なので例外コメント）と、当たらなかった `Path::is_file` という名前で見る道具の穴 |
| [worklog/2026-09-18-crates-io.md](worklog/2026-09-18-crates-io.md) | git 依存を crates.io の版に替えて初公開できる形にした記録（P1）: 下限の版（`sabiruby` 0.5.1、`sabiruby-compiler` 0.2.2）をその出どころごと manifest のコメントに残したこと、`rust-version` を `cargo metadata` から測って 1.95.0（bevy 0.19.1 が最大）と決めたこと、`categories` を crates.io の一覧の説明と突き合わせて `game-development` 1 つに絞った理由、下限と最新の両方で 75 passed だったこと、1 度だけ落ちて 4 回再現しなかった `tests/child_task.rs` の揺れ、`cargo tree -d` に `sabiruby` が出ない（出る hashbrown の重複は前からのもの）こと、docs.rs 向けに rustdoc の警告 3 つを素のコード span にして 0 にしたこと、そして `exclude` を足さなかった判断（87 ファイル・圧縮 296.2 KiB は上限 10 MiB の 3% 弱） |
| [worklog/2026-09-20-factory-survey.md](worklog/2026-09-20-factory-survey.md) | 調査のみ（`src/` 無変更）: rubevy_games の 3 本目（工場）の前に、**大量のイベント**と**スクリプト付きエンティティ数百〜数千台**を測った記録。購読キューの 64 は測った数ではなく変えられず、あふれは古い方から黙って落ちること、`publish` が購読リストを毎回全走査して購読者ごとに値を作り直すこと、予算は VM に 1 組で同じ優先度なら飢えないこと、`frame_time` が上限になっていないこと（設定値の 2〜4 倍）、同じ `.mrb` を N 台に載せると irep が N 部で差し替えのたびに積み上がること。実測の表（購読 1 本は 64 件/フレームが天井、毎フレーム動く形は約 1000 台で 8.3 ms、全台が同じ長さ眠ると p95 が 20 ms）、汎用の不足 A〜H と足すとしたらの形（著者判断待ち）、常設する価値のある計測 example 2 本（worktree `rubevy-wt-factory-survey` に未コミット）、測っていないこと |
